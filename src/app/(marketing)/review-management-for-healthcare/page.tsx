import ClinicPage, { metadata as clinicMetadata } from "../review-management-for-clinics/page";

export const metadata = {
    ...clinicMetadata,
    title: "Review Management Software for Healthcare | ReviewManagement",
};

export default ClinicPage;
